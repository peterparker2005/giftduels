// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: queries.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createUser = `-- name: CreateUser :one
INSERT INTO users (
    telegram_id,
    username,
    first_name,
    last_name,
    photo_url,
    language_code,
    allows_write_to_pm,
    is_premium
) VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, telegram_id, username, first_name, last_name, photo_url, language_code, allows_write_to_pm, is_premium, created_at, updated_at
`

type CreateUserParams struct {
	TelegramID      int64
	Username        pgtype.Text
	FirstName       string
	LastName        pgtype.Text
	PhotoUrl        pgtype.Text
	LanguageCode    pgtype.Text
	AllowsWriteToPm pgtype.Bool
	IsPremium       pgtype.Bool
}

func (q *Queries) CreateUser(ctx context.Context, arg CreateUserParams) (User, error) {
	row := q.db.QueryRow(ctx, createUser,
		arg.TelegramID,
		arg.Username,
		arg.FirstName,
		arg.LastName,
		arg.PhotoUrl,
		arg.LanguageCode,
		arg.AllowsWriteToPm,
		arg.IsPremium,
	)
	var i User
	err := row.Scan(
		&i.ID,
		&i.TelegramID,
		&i.Username,
		&i.FirstName,
		&i.LastName,
		&i.PhotoUrl,
		&i.LanguageCode,
		&i.AllowsWriteToPm,
		&i.IsPremium,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getUserByTelegramID = `-- name: GetUserByTelegramID :one
SELECT id, telegram_id, username, first_name, last_name, photo_url, language_code, allows_write_to_pm, is_premium, created_at, updated_at FROM users WHERE telegram_id = $1 LIMIT 1
`

func (q *Queries) GetUserByTelegramID(ctx context.Context, telegramID int64) (User, error) {
	row := q.db.QueryRow(ctx, getUserByTelegramID, telegramID)
	var i User
	err := row.Scan(
		&i.ID,
		&i.TelegramID,
		&i.Username,
		&i.FirstName,
		&i.LastName,
		&i.PhotoUrl,
		&i.LanguageCode,
		&i.AllowsWriteToPm,
		&i.IsPremium,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getUserByUsername = `-- name: GetUserByUsername :one
SELECT id, telegram_id, username, first_name, last_name, photo_url, language_code, allows_write_to_pm, is_premium, created_at, updated_at FROM users WHERE username = $1 LIMIT 1
`

func (q *Queries) GetUserByUsername(ctx context.Context, username pgtype.Text) (User, error) {
	row := q.db.QueryRow(ctx, getUserByUsername, username)
	var i User
	err := row.Scan(
		&i.ID,
		&i.TelegramID,
		&i.Username,
		&i.FirstName,
		&i.LastName,
		&i.PhotoUrl,
		&i.LanguageCode,
		&i.AllowsWriteToPm,
		&i.IsPremium,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateUser = `-- name: UpdateUser :one
UPDATE users SET
    username = $2,
    first_name = $3,
    last_name = $4,
    photo_url = $5,
    language_code = $6,
    allows_write_to_pm = $7,
    is_premium = $8
WHERE telegram_id = $1 RETURNING id, telegram_id, username, first_name, last_name, photo_url, language_code, allows_write_to_pm, is_premium, created_at, updated_at
`

type UpdateUserParams struct {
	TelegramID      int64
	Username        pgtype.Text
	FirstName       string
	LastName        pgtype.Text
	PhotoUrl        pgtype.Text
	LanguageCode    pgtype.Text
	AllowsWriteToPm pgtype.Bool
	IsPremium       pgtype.Bool
}

func (q *Queries) UpdateUser(ctx context.Context, arg UpdateUserParams) (User, error) {
	row := q.db.QueryRow(ctx, updateUser,
		arg.TelegramID,
		arg.Username,
		arg.FirstName,
		arg.LastName,
		arg.PhotoUrl,
		arg.LanguageCode,
		arg.AllowsWriteToPm,
		arg.IsPremium,
	)
	var i User
	err := row.Scan(
		&i.ID,
		&i.TelegramID,
		&i.Username,
		&i.FirstName,
		&i.LastName,
		&i.PhotoUrl,
		&i.LanguageCode,
		&i.AllowsWriteToPm,
		&i.IsPremium,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertUser = `-- name: UpsertUser :one
INSERT INTO users (
    telegram_id,
    username,
    first_name,
    last_name,
    photo_url,
    language_code,
    allows_write_to_pm,
    is_premium
) VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
ON CONFLICT (telegram_id) DO UPDATE SET
    username = EXCLUDED.username,
    first_name = EXCLUDED.first_name,
    last_name = EXCLUDED.last_name,
    photo_url = EXCLUDED.photo_url,
    language_code = EXCLUDED.language_code,
    allows_write_to_pm = EXCLUDED.allows_write_to_pm,
    is_premium = EXCLUDED.is_premium,
    updated_at = CURRENT_TIMESTAMP
RETURNING id, telegram_id, username, first_name, last_name, photo_url, language_code, allows_write_to_pm, is_premium, created_at, updated_at
`

type UpsertUserParams struct {
	TelegramID      int64
	Username        pgtype.Text
	FirstName       string
	LastName        pgtype.Text
	PhotoUrl        pgtype.Text
	LanguageCode    pgtype.Text
	AllowsWriteToPm pgtype.Bool
	IsPremium       pgtype.Bool
}

func (q *Queries) UpsertUser(ctx context.Context, arg UpsertUserParams) (User, error) {
	row := q.db.QueryRow(ctx, upsertUser,
		arg.TelegramID,
		arg.Username,
		arg.FirstName,
		arg.LastName,
		arg.PhotoUrl,
		arg.LanguageCode,
		arg.AllowsWriteToPm,
		arg.IsPremium,
	)
	var i User
	err := row.Scan(
		&i.ID,
		&i.TelegramID,
		&i.Username,
		&i.FirstName,
		&i.LastName,
		&i.PhotoUrl,
		&i.LanguageCode,
		&i.AllowsWriteToPm,
		&i.IsPremium,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
